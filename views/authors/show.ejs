<h2 class="page-header"><%= author.name %></h2>
<div class="btn-row">
    <a class="btn btn-primary" href="/authors/<%= author.edit %>/edit">Editar</a>
    <%- include('../partials/deleteForm', { url: `/authors/${author.id}` }) %>
</div>
<!--Si hay libros del autor los muestra-->
<% if (booksByAuthor.length > 0 )  { %>
    <h2 class="page-header">Libros del autor</h2>
    <%- include('../partials/bookGrid', { books: booksByAuthor }) %>
<% } %>
